# M1 驗收報告 — PDAF DCC 校正工具(離線階段)

> 日期:2026-07-16 · 驗收基準 commit:`bdd00f5` · 撰寫:M1 收尾驗收盤點
> 驗證環境:Linux 6.8 / GCC 11.4 / CMake 4.4.0 / Ninja;依賴 FetchContent 鎖版
> (ImGui v1.91.9b-docking、ImPlot v0.17、GLFW 3.4、Catch2、nlohmann_json)

## 1. 結論

**建議驗收通過**。M1 範圍(離線校正引擎 + CLI/GUI 工作台 + Sim 序列生成 + 獨立檢視器)
之功能需求全數實作並經測試閉環;80 案例全綠、build 零警告、CLI/GUI/seqview 煙霧測試通過。
驗收盤點期間發現一項覆蓋缺口(FR-15 有實作無專屬測試),已當場補測關閉(§4.3)。

尚待人工完成:SPEC 系列三方 review 簽核(校正/演算法/產線;SPEC-005 §6 出場準則第一條),
簽核欄見 §8。

## 2. 驗收範圍

**入列(M1)**:Phase A–G 離線工序(sweep 規劃、disp_seq.json 讀取/驗證、單位轉換、
粒度聚合、逐區回歸、focus 峰值、交叉驗證、EEPROM 打包 + 可讀等價檔)、
CLI `dcc_cal` 與 GUI `dcc_gui`(能力等價)、Sim 合成序列工作台、
獨立檢視器 `dcc_seqview`(外部 SAD 團隊自檢用)、報告/log/失敗落盤。

**不入列(M2/M3 或外部)**:實機擷取(FR-05/06)、SAD disparity 計算(FR-10)、
EEPROM 實體燒錄、HT/AT(重複性、節拍、CDAF 金標)、條件式 Qualcomm layout 對齊。

## 3. 驗證證據(2026-07-16 實測,commit `bdd00f5`)

| 項目 | 準則 | 實測 | 判定 |
|---|---|---|---|
| 建置 | 本專案 targets 零警告 | `--clean-first` 全量重建,0 警告 | ✅ |
| 測試 | ctest 全綠 | **80/80 passed**(0.6 s) | ✅ |
| CLI | `--dry-run` PASS、五檔齊全 | PASS;report.json/md + block.bin/json/txt | ✅ |
| GUI | `--smoke` 正常結束 | OK | ✅ |
| seqview | `--smoke` 正常結束 | OK | ✅ |
| 離線節拍 | NFR-01 離線 Phase D–G ≤ 1 s | dry-run 全程 < 0.01 s | ✅ |
| 離線重算 | NFR-03/IT-06 bit-exact | IT-06 案例 PASS(同序列重跑報告逐 bit 一致) | ✅ |

## 4. 測試對映(SPEC-005 §3/§4 ↔ 實際案例)

### 4.1 數量閉環驗算(鐵律 5)

規格對映 50(UT 40 + eeprom_equiv 2 + FR-15 補測 1 + IT 7)
+ 補充覆蓋 30(sim 注入/quality 7、OQ#3 靈敏度 4、config 4、raw 3、logging 1、seqview 11)
= **80 = ctest 案例總數** ✓

### 4.2 規格對映明細

| SPEC-005 ID | 對象(FR) | 案例數 | 狀態 |
|---|---|---|---|
| UT-01 | sweep 規劃(FR-03/04) | 5 | ✅(margin 0/0.05/0.1 端點、E-C01、E-A01) |
| UT-02 | 序列讀取(FR-08) | 9 | ✅(E-D01×4、E-D02×5) |
| UT-03 | 單位轉換(FR-09) | 1 | ✅(×16 後 bit-exact) |
| UT-04 | 無效樣本(FR-11) | 3 | ✅(7/8 點邊界、門檻可調) |
| UT-05 | regression(FR-12) | 3 | ✅(k=12.46/b=420 至 1e-6、E-E01) |
| UT-06 | focus 峰值(FR-13) | 3 | ✅(420±1、兩端 E-F01) |
| UT-07 | validate(FR-14) | 2 | ✅(0.199/0.200/0.201 邊界、最差三區排序) |
| UT-08 | eeprom(FR-16) | 5 | ✅(0x031D、checksum、round-trip、E-G01) |
| UT-09 | 單位守則(SPEC-004 §3) | 5 | ✅(ratio_check 漏乘/重複乘示警) |
| UT-10 | 粒度聚合(D-5) | 4 | ✅(144×108 median/weighted、0.49/0.50 邊界) |
| UT-補充 | eeprom_equiv(block.json/txt) | 2 | ✅(與 pack 同源閉環,993 bytes) |
| UT-補充 | 平滑性警告(FR-15) | 1 | ✅(本次驗收補測,見 §4.3) |
| IT-01 | 標準 dry-run | 1 | ✅(DCC 誤差 <3%、r²>0.99) |
| IT-02 | 角落無效樣本 | 1 | ✅(2 幀 PASS / 3 幀 E-D03) |
| IT-03 | L/R 對調 | 1 | ✅(E-E01 + abort_dump.json 落盤,鐵律 4) |
| IT-04 | chart 距離錯 | 1 | ✅(E-F01) |
| IT-05 | 曝光過高幀 | — | **[M2]** 屬實機擷取,依規劃延後 |
| IT-06 | 離線重算 + 五檔落盤 | 2 | ✅(bit-exact;五檔 + block.json≡block.bin 閉環) |
| IT-07 | 誤差預算閉環(§3a) | 1 | ✅(σ=0.5+bias=0.3 ×10 蒙地卡羅:CV<2%、偏差<3%) |

### 4.3 驗收盤點發現與處置

- **FR-15(DCC 平滑性警告)有實作、無專屬測試**:實作位於 `pipeline.cpp:76-88`
  (config `smooth_limit` 預設 0.25、GUI 可調),但 79 案例中無一驗證其行為。
  已補測(commit `bdd00f5`):預設梯度不誤報、門檻 0.01 觸發警告含區座標、
  警告不影響 PASS 判定。**除此之外未發現規格—實作—測試三方不一致。**

## 5. 需求覆蓋(SPEC-001)

| 需求 | 覆蓋方式 | 狀態 |
|---|---|---|
| FR-01 config 載入/驗證 | config 4 案例(缺欄位/非法值 E-A01、交叉約束、serialize 閉環) | ✅ |
| FR-02 前置條件 | UT-01(INF≥MACRO → E-A01)、config 驗證器 | ✅ |
| FR-03 sweep 公式 | UT-01(禁止寫死,margin 參數化) | ✅ |
| FR-04 FAR→NEAR 單向 | 設計面保證:`sweep::plan` 無方向參數(鐵律 3) | ✅ |
| FR-05/06 實機擷取 | — | [M2/外部] |
| FR-07 HAL 介面注入 | SimHAL 貫穿 dry-run(IT-01);介面見 SPEC-003 §4 | ✅ |
| FR-08 序列讀取/驗證 | UT-02(D-1..D-6 判定)、UT-10(聚合) | ✅ |
| FR-09 入向單位轉換 | UT-03、UT-09(全案僅讀取端/打包端兩處,鐵律 1) | ✅ |
| FR-10 SAD 計算 | 外部承擔;介面驗收條款 §3a 由 IT-07 本端閉環 | [外部] |
| FR-11 無效樣本 | UT-04 | ✅ |
| FR-12 逐區回歸 | UT-05 | ✅ |
| FR-13 focus 峰值 | UT-06 | ✅ |
| FR-14 交叉驗證 | UT-07 + focus_peak_offset/field_curvature 演練 | ✅ |
| FR-15 平滑性警告 | 本次補測(§4.3) | ✅ |
| FR-16 EEPROM 打包 | UT-08 + SimNvm 回讀;可讀等價檔 SPEC-004 §4a | ✅ |
| FR-17 單模組報告 | IT-06 五檔;report 雙單位 + ratio_check | ✅ |
| FR-18 RAW 唯讀載入 | load_raw/generate_raw 3 案例(缺檔降級) | ✅ |
| NFR-01 節拍 | 離線 ≤1 s:實測 <0.01 s;實機 ≤4 s 屬 HT-02 [M2] | ✅(離線) |
| NFR-02 重複性 CV<2% | IT-07 蒙地卡羅代理;實機 HT-01 [M2] | ✅(代理) |
| NFR-03 可追溯 | Logger 雙 sink(log+jsonl)、IT-06 離線重算、失敗 abort_dump | ✅ |
| NFR-04 可移植 | dcc_core 純 C++17(無 I/O/UI/執行緒/json 依賴),分層單向 | ✅ |
| NFR-05 繁中 UTF-8 | 報告/log/GUI 全繁中;glyph 首幀自檢 | ✅ |

## 6. 鐵律遵循抽查

1. **單位契約**:轉換僅 `disparity_reader`(入)與打包端(出)兩處;UT-09 ratio_check 釘死。
2. **分層**:core 無外部依賴;依賴方向 gui/cli → app → io/hal → core(建置系統強制)。
3. **掃描方向**:`sweep::plan` 無反轉參數。
4. **失敗落盤**:IT-03 驗證 abort_dump.json;錯誤碼已收斂為具名碼
   (E-A01/C01/D01..03/E01/F01/G01,SPEC-005 §8 #3 之 M1 出口收斂完成)。
5. **驗算文化**:案例數 50+30=80 閉環(§4.1);block 993 bytes 閉環(UT-補充);
   288×432 尺寸鏈見 PhaseD-1 圖解。

## 7. 遺留事項與 M2 入口條件

- 介面凍結完成:輸入 disp_seq.json v1(2026-07-16)、輸出 EEPROM layout 轉正 + block.json/txt。
- 開放問題(SPEC-005 §7):#5 已關閉;#1/#2/#4 明確延期至 M2;#3 分析框架已備,
  待實模組非線性量測。**出場準則「全數關閉或明確延期」成立。**
- M2 待辦清單見 `docs/開發紀錄_M0-M1.md` §7(實機 HAL、NvmIF、q→σ 標定、HT/AT)。
- 已知限制:Sim focus 面無獨立雜訊注入(明確延期,開發紀錄 §7 #7);
  IT-05 需實機擷取,屬 M2。

## 8. 簽核(SPEC-005 §6 出場準則)

| 角色 | 簽核 | 日期 |
|---|---|---|
| 校正 | ______ | ______ |
| 演算法 | ______ | ______ |
| 產線 | ______ | ______ |
